fn fib(n: u32) -> i128 {
    let x: i128 = 0;
    let y: i128 = 1;
    for i in 0..n {
        let tmp: i128 = x;
        x = y;
        y = tmp + y;
    }
    return x;
}

let result: i128 = fib(184);

println(result);
