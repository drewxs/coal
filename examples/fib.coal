fn fib(n: int) -> int {
    let i: int = 0;
    let x: int = 0;
    let y: int = 1;

    while i < n {
        let tmp: int = x;
        x = y;
        y = tmp + y;
        i = i + 1;
    }

    return x;
}

fib(50);

