struct User {
    id: u64;
    email: str;
    enabled: bool = true;

    fn new(id: u64, email: str) {
        println("hello, world!");
    }

}

fn main() {
    let u: User = User {
        id: 1,
        email: "foo@bar.com",
    };

    println(u);
}

